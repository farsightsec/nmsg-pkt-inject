nmsg-pkt-inject: libpcap frame injection utility for NMSG ISC/pkt encoded data
==============================================================================

This is nmsg-pkt-inject, a utility which reads packets encoded using the NMSG
ISC/pkt message type and injects them onto a live ethernet interface using the
libpcap pcap_inject() function.
